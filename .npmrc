# VoidGlass is published to the homelab Forgejo registry, not npmjs.com. Reads
# are anonymous, so no token is needed to install - only to publish.
@shychedelic:registry=https://git.lab.shychedelic.com/api/packages/shychedelic/npm/
