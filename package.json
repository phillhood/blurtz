{
  "name": "blurtz",
  "description": "Blurtz - a real-time multiplayer Nertz game",
  "version": "1.0.0",
  "author": "Phill Hood",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace shared",
    "build": "npm run build:shared && npm run build --workspace server && npm run build --workspace client",
    "pretest": "npm run build:shared",
    "test": "npm run test --workspace shared && npm run test --workspace server && npm run test:run --workspace client",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "globals": "^15.15.0",
    "typescript-eslint": "^8.64.0"
  }
}
